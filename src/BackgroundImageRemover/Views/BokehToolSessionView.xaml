<UserControl x:Class="BackgroundImageRemover.Views.BokehToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="● BOKEH" Accent="#FF06B6D4" />

<controls:ToolSessionPanel Title="Bokeh" Description="Scatters soft blurred circles over the image for a dreamy depth-of-field look." >
        <controls:ToolSessionPanel.Panel>
<StackPanel>

                    <controls:ColorPickerField Header="Circle color" SelectedColor="{Binding Color}" />

                    <controls:SliderField Header="Radius" Value="{Binding Radius}" Minimum="2" Maximum="60" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
<controls:SliderField Header="Count" Value="{Binding Count}" Minimum="0" Maximum="400" TickFrequency="10"  />
<controls:SliderField Header="Opacity" Value="{Binding Opacity}" Minimum="0" Maximum="1" SmallChange="0.05" ValueFormat="{}{0:P0}"  />
<controls:SliderField Header="Edge blur" Value="{Binding Blur}" Minimum="0" Maximum="24" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
</StackPanel>
</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
