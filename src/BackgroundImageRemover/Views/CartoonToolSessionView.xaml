<UserControl x:Class="BackgroundImageRemover.Views.CartoonToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="🎭 CARTOON" Accent="#FFA855F7" />

<controls:ToolSessionPanel Title="Cartoon" Description="Smooths the image into flat colors and adds dark outlines for a cartoon look." >
        <controls:ToolSessionPanel.Panel>

                    <controls:SliderField Header="Smoothness" Value="{Binding Smoothness}" Minimum="1" Maximum="20" TickFrequency="1" IsSnapToTickEnabled="True"  />
<controls:SliderField Header="Color levels" Value="{Binding QuantizeLevels}" Minimum="2" Maximum="32" TickFrequency="1" IsSnapToTickEnabled="True"  />
<controls:SliderField Header="Outline strength" Value="{Binding EdgeThreshold}" Minimum="0" Maximum="40" TickFrequency="1" IsSnapToTickEnabled="True"  />
</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
