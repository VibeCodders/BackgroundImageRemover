<UserControl x:Class="BackgroundImageRemover.Views.ColorReplaceToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="🎨 COLOR REPLACE" Accent="#FFEC4899" />

<controls:ToolSessionPanel Title="Color Replace" Description="Click the image to pick the target color, or choose it below." >
        <controls:ToolSessionPanel.Panel>

                    <controls:ColorPickerField Header="Target color" SelectedColor="{Binding TargetColor}" />

                    <controls:ColorPickerField Header="Replacement color" SelectedColor="{Binding ReplacementColor}" />

                    <controls:SliderField Header="Tolerance" Value="{Binding Tolerance}" Minimum="0" Maximum="1" SmallChange="0.05" ValueFormat="{}{0:P0}"  />
<controls:SliderField Header="Softness" Value="{Binding Softness}" Minimum="0" Maximum="1" SmallChange="0.05" ValueFormat="{}{0:P0}"  />
<CheckBox Content="Preserve luminance" IsChecked="{Binding PreserveLuminance}" Margin="0,0,0,16" />

</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl x:Name="Preview"
                                               ImageSource="{Binding ResultBitmap}"
                                               MouseLeftButtonDown="Preview_MouseLeftButtonDown" />
            </Border>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
