<UserControl x:Class="BackgroundImageRemover.Views.Controls.SliderField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="48" d:DesignWidth="272">
    <StackPanel>
        <DockPanel Margin="0,4,0,0">
            <TextBlock Text="{Binding Header, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontWeight="SemiBold" DockPanel.Dock="Left" />
            <TextBlock Text="{Binding ValueText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
        </DockPanel>
        <Slider Minimum="{Binding Minimum, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Maximum="{Binding Maximum, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Value="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                SmallChange="{Binding SmallChange, RelativeSource={RelativeSource AncestorType=UserControl}}"
                LargeChange="{Binding LargeChange, RelativeSource={RelativeSource AncestorType=UserControl}}"
                TickFrequency="{Binding TickFrequency, RelativeSource={RelativeSource AncestorType=UserControl}}"
                IsSnapToTickEnabled="{Binding IsSnapToTickEnabled, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Margin="0,2,0,8" />
    </StackPanel>
</UserControl>
