<UserControl x:Class="BackgroundImageRemover.Views.CropToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             xmlns:models="clr-namespace:BackgroundImageRemover.Models"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="✂ CROP" Accent="#FF16A34A" />

        <StatusBar DockPanel.Dock="Bottom" Background="{DynamicResource Theme.BarBackground}" TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
            <StatusBarItem><TextBlock Text="{Binding StatusMessage}" /></StatusBarItem>
        </StatusBar>

        <Border DockPanel.Dock="Right" Width="300" Padding="14" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1,0,0,0" Background="{DynamicResource Theme.PanelBackground}" TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Crop" FontSize="15" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource Theme.ForegroundBrush}" />

                    <TextBlock Text="Aspect ratio" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding AspectPresets}" DisplayMemberPath="Label" SelectedItem="{Binding SelectedAspect}" Margin="0,0,0,12" />

                    <TextBlock Text="Margins (px)" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" /><ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" /><ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,4,0"><TextBlock Text="L" /><TextBox Text="{Binding MarginLeft, UpdateSourceTrigger=PropertyChanged}" /></StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,4,0"><TextBlock Text="T" /><TextBox Text="{Binding MarginTop, UpdateSourceTrigger=PropertyChanged}" /></StackPanel>
                        <StackPanel Grid.Column="2" Margin="0,0,4,0"><TextBlock Text="R" /><TextBox Text="{Binding MarginRight, UpdateSourceTrigger=PropertyChanged}" /></StackPanel>
                        <StackPanel Grid.Column="3"><TextBlock Text="B" /><TextBox Text="{Binding MarginBottom, UpdateSourceTrigger=PropertyChanged}" /></StackPanel>
                    </Grid>
                    <Button Content="Apply margins" Command="{Binding ApplyMarginsCommand}" Margin="0,0,0,12" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Straighten" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding Angle, StringFormat='{}{0:0}°'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="-45" Maximum="45" Value="{Binding Angle}" Margin="0,2,0,8" />
                    <Button Content="Auto-straighten (deskew)" Command="{Binding AutoStraightenCommand}" Margin="0,0,0,12" />

                    <TextBlock Text="Trim to color" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <UniformGrid Columns="3" Margin="0,0,0,12">
                        <Button Content="Border" Command="{Binding AutoTrimCommand}" Margin="0,0,4,0" />
                        <Button Content="White" Command="{Binding TrimWhiteCommand}" Margin="4,0,4,0" />
                        <Button Content="Black" Command="{Binding TrimBlackCommand}" Margin="4,0,0,0" />
                    </UniformGrid>

                    <TextBlock Text="Center crop to size (px)" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,4,0">
                            <TextBlock Text="W" />
                            <TextBox Text="{Binding TargetWidth, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="H" />
                            <TextBox Text="{Binding TargetHeight, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>
                    <Button Content="Apply size" Command="{Binding ApplySizeCommand}" Margin="0,0,0,12" />

                    <TextBlock Text="Rotate" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <UniformGrid Columns="2" Margin="0,0,0,12">
                        <Button Content="↺ 90°" Command="{Binding Rotate90CounterClockwiseCommand}" Margin="0,0,4,0" />
                        <Button Content="↻ 90°" Command="{Binding Rotate90ClockwiseCommand}" Margin="4,0,0,0" />
                    </UniformGrid>

                    <Button Content="↺ Reset" Command="{Binding ResetCommand}" />
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" /><ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="0" Margin="8">
                <TextBlock DockPanel.Dock="Top" Text="Drag to select crop area" FontWeight="Bold" Margin="0,0,0,4" />
                <controls:ImagePreviewControl x:Name="CropPreview"
                                               ImageSource="{Binding SourceBitmap}"
                                               Mode="{x:Static models:InteractionMode.DrawRect}"
                                               RectSelected="CropPreview_RectSelected" />
            </DockPanel>
            <DockPanel Grid.Column="1" Margin="8">
                <TextBlock DockPanel.Dock="Top" Text="Result" FontWeight="Bold" Margin="0,0,0,4" />
                <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                    <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
                </Border>
            </DockPanel>
        </Grid>
    </DockPanel>
</UserControl>
