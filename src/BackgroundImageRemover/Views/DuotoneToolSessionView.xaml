<UserControl x:Class="BackgroundImageRemover.Views.DuotoneToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="◐ DUOTONE" Accent="#FF8B5CF6" />

<controls:ToolSessionPanel Title="Duotone" >
        <controls:ToolSessionPanel.Panel>
<StackPanel>

                    <TextBlock Text="Presets" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ItemsControl ItemsSource="{Binding Presets}" Margin="0,0,0,12">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.ApplyPresetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" Margin="0,0,6,6" Padding="6,4"
                                        Background="{DynamicResource Theme.PanelHeaderBackground}" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1"
                                        Foreground="{DynamicResource Theme.ForegroundBrush}" Cursor="Hand">
                                    <StackPanel Orientation="Horizontal">
                                        <Rectangle Width="14" Height="14" Fill="{Binding Dark, Converter={StaticResource ColorToBrushConverter}}" Stroke="Gray" VerticalAlignment="Center" Margin="0,0,2,0" />
                                        <Rectangle Width="14" Height="14" Fill="{Binding Light, Converter={StaticResource ColorToBrushConverter}}" Stroke="Gray" VerticalAlignment="Center" Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <controls:ColorPickerField Header="Dark color" SelectedColor="{Binding DarkColor}" />

                    <controls:ColorPickerField Header="Light color" SelectedColor="{Binding LightColor}" />

                    <controls:SliderField Header="Midpoint" Value="{Binding Midpoint}" Minimum="0" Maximum="1" SmallChange="0.05" ValueFormat="{}{0:P0}"  />
<controls:SliderField Header="Strength" Value="{Binding Strength}" Minimum="0" Maximum="1" SmallChange="0.05" ValueFormat="{}{0:P0}"  />
</StackPanel>
</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
