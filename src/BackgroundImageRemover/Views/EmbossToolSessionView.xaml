<UserControl x:Class="BackgroundImageRemover.Views.EmbossToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="🗻 EMBOSS" Accent="#FF92400E" />

<controls:ToolSessionPanel Title="Emboss" Description="Raises edges into a relief. The angle sets the direction the light comes from." >
        <controls:ToolSessionPanel.Panel>
<StackPanel>

                    <controls:SliderField Header="Light angle" Value="{Binding Angle}" Minimum="0" Maximum="315" TickFrequency="45" IsSnapToTickEnabled="True" ValueFormat="{}{0:0}°"  />
<controls:SliderField Header="Strength" Value="{Binding Strength}" Minimum="0.2" Maximum="5" SmallChange="0.1" ValueFormat="{}{0:0.0}x"  />
<CheckBox Content="Grayscale (classic emboss)" IsChecked="{Binding Grayscale}" Margin="0,0,0,16" />

</StackPanel>
</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
