<UserControl x:Class="BackgroundImageRemover.Views.FiltersToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             xmlns:models="clr-namespace:BackgroundImageRemover.Models"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="FilterKindValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:FilterKind" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader DockPanel.Dock="Top" Badge="🎨 FILTERS" Accent="#FFD946EF" />

<controls:ToolSessionPanel Title="Artistic Filters" >
        <controls:ToolSessionPanel.Panel>
<StackPanel>
                    <TextBlock Text="Filter" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource FilterKindValues}}" SelectedItem="{Binding SelectedFilter}" Margin="0,0,0,12" />

                    <StackPanel Visibility="{Binding HasEffect, Converter={StaticResource BoolToVisibilityConverter}}">
                        <controls:SliderField Header="Intensity" Value="{Binding Intensity}" Minimum="0" Maximum="1" SmallChange="0.05" LargeChange="0.1" ValueFormat="{}{0:P0}"  />
</StackPanel>

                    <StackPanel Visibility="{Binding SelectedFilter, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Posterize}">
                        <controls:SliderField Header="Levels" Value="{Binding PosterizeLevels}" Minimum="2" Maximum="16" TickFrequency="1" IsSnapToTickEnabled="True"  />
</StackPanel>

</StackPanel>
</controls:ToolSessionPanel.Panel>

    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
