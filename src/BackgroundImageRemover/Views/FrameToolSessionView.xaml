<UserControl x:Class="BackgroundImageRemover.Views.FrameToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundBrush}" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundBrush}" />
        </Style>
    </UserControl.Resources>
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <Border DockPanel.Dock="Top" Background="{DynamicResource Theme.BarBackground}" Padding="12,8" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" DockPanel.Dock="Left">
                    <Border Background="#FF7C3AED" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                        <TextBlock Text="▣ FRAME" Foreground="White" FontWeight="Bold" FontSize="11" />
                    </Border>
                    <TextBlock Text="{Binding Title}" Foreground="{DynamicResource Theme.ForegroundBrush}" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <Button Content="✕ Cancel (Esc)" Command="{Binding CancelCommand}" Padding="12,6" Margin="0,0,8,0" Background="{DynamicResource Theme.PanelBackground}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1" Cursor="Hand" FontWeight="SemiBold" />
                    <Button Content="✓ Apply (Enter)" Command="{Binding ApplyCommand}" Padding="16,6" Background="#FF7C3AED" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="Bold" />
                </StackPanel>
            </DockPanel>
        </Border>

        <StatusBar DockPanel.Dock="Bottom" Background="{DynamicResource Theme.BarBackground}" TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
            <StatusBarItem><TextBlock Text="{Binding StatusMessage}" /></StatusBarItem>
        </StatusBar>

        <Border DockPanel.Dock="Right" Width="300" Padding="14" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1,0,0,0" Background="{DynamicResource Theme.PanelBackground}" TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Frame &amp; Border" FontSize="15" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource Theme.ForegroundBrush}" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Border thickness" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding BorderThickness, StringFormat='{}{0}px'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0" Maximum="200" Value="{Binding BorderThickness}" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,2,0,4" />

                    <StackPanel Orientation="Horizontal" Margin="0,4,0,12">
                        <Rectangle Width="24" Height="24" Fill="{Binding BorderColor, Converter={StaticResource ColorToBrushConverter}}" Stroke="Gray" />
                        <Button x:Name="ChooseBorderColorButton" Content="Choose color..." Margin="8,0,0,0" Click="ChooseBorderColorButton_Click" />
                        <Popup IsOpen="{Binding IsColorPickerOpen}" PlacementTarget="{Binding ElementName=ChooseBorderColorButton}" StaysOpen="False" AllowsTransparency="True" Placement="Right">
                            <Border Background="{DynamicResource Theme.PopupBackground}" BorderBrush="{DynamicResource Theme.PopupBorderBrush}" BorderThickness="1">
                                <controls:SimpleColorPickerControl SelectedColor="{Binding BorderColor}" />
                            </Border>
                        </Popup>
                    </StackPanel>

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Border opacity" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding BorderOpacity, StringFormat='{}{0:P0}'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0" Maximum="1" Value="{Binding BorderOpacity}" TickFrequency="0.05" Margin="0,2,0,8" />

                    <TextBlock Text="Border sides" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <UniformGrid Columns="2" Margin="0,0,0,12">
                        <CheckBox Content="Top" IsChecked="{Binding BorderTop}" Margin="0,0,4,2" />
                        <CheckBox Content="Right" IsChecked="{Binding BorderRight}" Margin="4,0,0,2" />
                        <CheckBox Content="Bottom" IsChecked="{Binding BorderBottom}" Margin="0,0,4,0" />
                        <CheckBox Content="Left" IsChecked="{Binding BorderLeft}" Margin="4,0,0,0" />
                    </UniformGrid>

                    <CheckBox Content="Gradient border" IsChecked="{Binding GradientBorderEnabled}" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <StackPanel Margin="0,0,0,12"
                                Visibility="{Binding GradientBorderEnabled, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Start color" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Rectangle Width="24" Height="24" Fill="{Binding GradientBorderColorA, Converter={StaticResource ColorToBrushConverter}}" Stroke="Gray" />
                            <Button x:Name="ChooseGradientColorAButton" Content="Choose..." Margin="8,0,0,0" Click="ChooseGradientColorAButton_Click" />
                            <Popup IsOpen="{Binding IsGradientColorAPickerOpen}" PlacementTarget="{Binding ElementName=ChooseGradientColorAButton}" StaysOpen="False" AllowsTransparency="True" Placement="Right">
                                <Border Background="{DynamicResource Theme.PopupBackground}" BorderBrush="{DynamicResource Theme.PopupBorderBrush}" BorderThickness="1">
                                    <controls:SimpleColorPickerControl SelectedColor="{Binding GradientBorderColorA}" />
                                </Border>
                            </Popup>
                        </StackPanel>
                        <TextBlock Text="End color" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="24" Height="24" Fill="{Binding GradientBorderColorB, Converter={StaticResource ColorToBrushConverter}}" Stroke="Gray" />
                            <Button x:Name="ChooseGradientColorBButton" Content="Choose..." Margin="8,0,0,0" Click="ChooseGradientColorBButton_Click" />
                            <Popup IsOpen="{Binding IsGradientColorBPickerOpen}" PlacementTarget="{Binding ElementName=ChooseGradientColorBButton}" StaysOpen="False" AllowsTransparency="True" Placement="Right">
                                <Border Background="{DynamicResource Theme.PopupBackground}" BorderBrush="{DynamicResource Theme.PopupBorderBrush}" BorderThickness="1">
                                    <controls:SimpleColorPickerControl SelectedColor="{Binding GradientBorderColorB}" />
                                </Border>
                            </Popup>
                        </StackPanel>
                    </StackPanel>

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Corner radius" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding CornerRadius, StringFormat='{}{0}px'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0" Maximum="400" Value="{Binding CornerRadius}" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,2,0,12" />

                    <TextBlock Text="Padding (px)" FontWeight="SemiBold" Margin="0,4,0,4" />
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Left" VerticalAlignment="Center" Margin="0,0,4,4" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,8,4" Text="{Binding PaddingLeft, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Top" VerticalAlignment="Center" Margin="0,0,4,4" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                        <TextBox Grid.Row="0" Grid.Column="3" Margin="0,0,0,4" Text="{Binding PaddingTop, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Right" VerticalAlignment="Center" Margin="0,0,4,0" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,8,0" Text="{Binding PaddingRight, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Grid.Row="1" Grid.Column="2" Text="Bottom" VerticalAlignment="Center" Margin="0,0,4,0" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                        <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding PaddingBottom, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>

                    <CheckBox Content="Mat color instead of transparent padding" IsChecked="{Binding UseMatColor}" Margin="0,0,0,8" FontWeight="SemiBold" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12"
                                Visibility="{Binding UseMatColor, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Rectangle Width="24" Height="24" Fill="{Binding MatColor, Converter={StaticResource ColorToBrushConverter}}" Stroke="Gray" />
                        <Button x:Name="ChooseMatColorButton" Content="Choose mat color..." Margin="8,0,0,0" Click="ChooseMatColorButton_Click" />
                        <Popup IsOpen="{Binding IsMatColorPickerOpen}" PlacementTarget="{Binding ElementName=ChooseMatColorButton}" StaysOpen="False" AllowsTransparency="True" Placement="Right">
                            <Border Background="{DynamicResource Theme.PopupBackground}" BorderBrush="{DynamicResource Theme.PopupBorderBrush}" BorderThickness="1">
                                <controls:SimpleColorPickerControl SelectedColor="{Binding MatColor}" />
                            </Border>
                        </Popup>
                    </StackPanel>

                    <TextBlock Text="Inner accent border" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <DockPanel Margin="0,0,0,4">
                        <TextBlock Text="Thickness" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding InnerBorderThickness, StringFormat='{}{0}px'}" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0" Maximum="60" Value="{Binding InnerBorderThickness}" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,0,0,4" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <Rectangle Width="24" Height="24" Fill="{Binding InnerBorderColor, Converter={StaticResource ColorToBrushConverter}}" Stroke="Gray" />
                        <Button x:Name="ChooseInnerColorButton" Content="Choose accent color..." Margin="8,0,0,0" Click="ChooseInnerColorButton_Click" />
                        <Popup IsOpen="{Binding IsInnerColorPickerOpen}" PlacementTarget="{Binding ElementName=ChooseInnerColorButton}" StaysOpen="False" AllowsTransparency="True" Placement="Right">
                            <Border Background="{DynamicResource Theme.PopupBackground}" BorderBrush="{DynamicResource Theme.PopupBorderBrush}" BorderThickness="1">
                                <controls:SimpleColorPickerControl SelectedColor="{Binding InnerBorderColor}" />
                            </Border>
                        </Popup>
                    </StackPanel>
                    <DockPanel Margin="0,0,0,12">
                        <TextBlock Text="Opacity" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding InnerBorderOpacity, StringFormat='{}{0:P0}'}" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>

                    <CheckBox Content="Bevel (3D edge)" IsChecked="{Binding BevelEnabled}" Margin="0,0,0,8" FontWeight="SemiBold" />
                    <StackPanel Visibility="{Binding BevelEnabled, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,0,0,12">
                        <DockPanel>
                            <TextBlock Text="Thickness" FontWeight="SemiBold" DockPanel.Dock="Left" />
                            <TextBlock Text="{Binding BevelThickness, StringFormat='{}{0}px'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                        </DockPanel>
                        <Slider Minimum="0" Maximum="40" Value="{Binding BevelThickness}" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,0,0,8" />
                        <DockPanel>
                            <TextBlock Text="Opacity" FontWeight="SemiBold" DockPanel.Dock="Left" />
                            <TextBlock Text="{Binding BevelOpacity, StringFormat='{}{0:P0}'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                        </DockPanel>
                        <Slider Minimum="0" Maximum="1" Value="{Binding BevelOpacity}" TickFrequency="0.05" />
                    </StackPanel>

                    <CheckBox Content="Outer shadow" IsChecked="{Binding OuterShadowEnabled}" Margin="0,0,0,8" FontWeight="SemiBold" />
                    <StackPanel Visibility="{Binding OuterShadowEnabled, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,0,0,12">
                        <DockPanel>
                            <TextBlock Text="Offset" FontWeight="SemiBold" DockPanel.Dock="Left" />
                            <TextBlock Text="{Binding OuterShadowOffset, StringFormat='{}{0:0}px'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                        </DockPanel>
                        <Slider Minimum="0" Maximum="60" Value="{Binding OuterShadowOffset}" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,0,0,8" />
                        <DockPanel>
                            <TextBlock Text="Blur" FontWeight="SemiBold" DockPanel.Dock="Left" />
                            <TextBlock Text="{Binding OuterShadowBlur, StringFormat='{}{0:0}px'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                        </DockPanel>
                        <Slider Minimum="0" Maximum="40" Value="{Binding OuterShadowBlur}" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,0,0,8" />
                        <DockPanel>
                            <TextBlock Text="Opacity" FontWeight="SemiBold" DockPanel.Dock="Left" />
                            <TextBlock Text="{Binding OuterShadowOpacity, StringFormat='{}{0:P0}'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                        </DockPanel>
                        <Slider Minimum="0" Maximum="1" Value="{Binding OuterShadowOpacity}" TickFrequency="0.05" />
                    </StackPanel>

                    <CheckBox Content="Polaroid caption bar" IsChecked="{Binding PolaroidEnabled}" Margin="0,0,0,8" FontWeight="SemiBold" />
                    <StackPanel Visibility="{Binding PolaroidEnabled, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,0,0,12">
                        <DockPanel>
                            <TextBlock Text="Height" FontWeight="SemiBold" DockPanel.Dock="Left" />
                            <TextBlock Text="{Binding PolaroidHeight, StringFormat='{}{0}px'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                        </DockPanel>
                        <Slider Minimum="0" Maximum="300" Value="{Binding PolaroidHeight}" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,0,0,8" />
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="24" Height="24" Fill="{Binding PolaroidColor, Converter={StaticResource ColorToBrushConverter}}" Stroke="Gray" />
                            <Button x:Name="ChoosePolaroidColorButton" Content="Choose color..." Margin="8,0,0,0" Click="ChoosePolaroidColorButton_Click" />
                            <Popup IsOpen="{Binding IsPolaroidColorPickerOpen}" PlacementTarget="{Binding ElementName=ChoosePolaroidColorButton}" StaysOpen="False" AllowsTransparency="True" Placement="Right">
                                <Border Background="{DynamicResource Theme.PopupBackground}" BorderBrush="{DynamicResource Theme.PopupBorderBrush}" BorderThickness="1">
                                    <controls:SimpleColorPickerControl SelectedColor="{Binding PolaroidColor}" />
                                </Border>
                            </Popup>
                        </StackPanel>
                    </StackPanel>

                    <CheckBox Content="Vignette" IsChecked="{Binding VignetteEnabled}" Margin="0,0,0,8" FontWeight="SemiBold" />
                    <StackPanel Visibility="{Binding VignetteEnabled, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,0,0,12">
                        <DockPanel>
                            <TextBlock Text="Strength" FontWeight="SemiBold" DockPanel.Dock="Left" />
                            <TextBlock Text="{Binding VignetteStrength, StringFormat='{}{0:P0}'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                        </DockPanel>
                        <Slider Minimum="0" Maximum="1" Value="{Binding VignetteStrength}" TickFrequency="0.05" Margin="0,0,0,8" />
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="24" Height="24" Fill="{Binding VignetteColor, Converter={StaticResource ColorToBrushConverter}}" Stroke="Gray" />
                            <Button x:Name="ChooseVignetteColorButton" Content="Choose color..." Margin="8,0,0,0" Click="ChooseVignetteColorButton_Click" />
                            <Popup IsOpen="{Binding IsVignetteColorPickerOpen}" PlacementTarget="{Binding ElementName=ChooseVignetteColorButton}" StaysOpen="False" AllowsTransparency="True" Placement="Right">
                                <Border Background="{DynamicResource Theme.PopupBackground}" BorderBrush="{DynamicResource Theme.PopupBorderBrush}" BorderThickness="1">
                                    <controls:SimpleColorPickerControl SelectedColor="{Binding VignetteColor}" />
                                </Border>
                            </Popup>
                        </StackPanel>
                    </StackPanel>

                    <Button Content="↺ Reset" Command="{Binding ResetCommand}" Height="30" Background="{DynamicResource Theme.PanelHeaderBackground}" Foreground="{DynamicResource Theme.ForegroundBrush}" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1" />
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
