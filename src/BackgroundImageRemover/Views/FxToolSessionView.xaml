<UserControl x:Class="BackgroundImageRemover.Views.FxToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader DockPanel.Dock="Top" Badge="✨ FX" Accent="#FFC026D3" />

<controls:ToolSessionPanel Title="Cinematic Effects" >
        <controls:ToolSessionPanel.Panel>
<StackPanel>

                    <controls:SliderField Header="Bloom" Value="{Binding BloomStrength}" Minimum="0" Maximum="1" SmallChange="0.05" ValueFormat="{}{0:P0}"  />
<controls:SliderField Header="Glow" Value="{Binding GlowStrength}" Minimum="0" Maximum="1" SmallChange="0.05" ValueFormat="{}{0:P0}"  />
<controls:SliderField Header="Light leak" Value="{Binding LightLeakStrength}" Minimum="0" Maximum="1" SmallChange="0.05" ValueFormat="{}{0:P0}"  />
<controls:SliderField Header="Chromatic aberration" Value="{Binding ChromaticAberrationStrength}" Minimum="0" Maximum="2" SmallChange="0.05" ValueFormat="{}{0:0.00}"  />
<TextBlock Text="Bokeh" FontWeight="SemiBold" Margin="0,8,0,4" />
                    <controls:SliderField Header="Count" Value="{Binding BokehCount}" Minimum="0" Maximum="200" TickFrequency="5" IsSnapToTickEnabled="True"  />
<controls:SliderField Header="Size" Value="{Binding BokehSize}" Minimum="1" Maximum="80" ValueFormat="{}{0:0}px"  />
</StackPanel>
</controls:ToolSessionPanel.Panel>

    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
