<UserControl x:Class="BackgroundImageRemover.Views.GlowToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader DockPanel.Dock="Top" Badge="✨ GLOW" Accent="#FFFFB300" />

<controls:ToolSessionPanel Title="Glow" Description="Bright areas radiate a soft halo of light, like a lens bloom." >
        <controls:ToolSessionPanel.Panel>
<StackPanel>

                    <controls:SliderField Header="Brightness threshold" Value="{Binding Threshold}" Minimum="0" Maximum="255" TickFrequency="5"  />
<controls:SliderField Header="Glow radius" Value="{Binding Radius}" Minimum="0" Maximum="60" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
<controls:SliderField Header="Strength" Value="{Binding Strength}" Minimum="0" Maximum="2" SmallChange="0.1" ValueFormat="{}{0:0.0}x"  />
</StackPanel>
</controls:ToolSessionPanel.Panel>

    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
