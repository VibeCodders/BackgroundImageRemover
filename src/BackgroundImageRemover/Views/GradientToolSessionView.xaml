<UserControl x:Class="BackgroundImageRemover.Views.GradientToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="◮ GRADIENT" Accent="#FFF59E0B" />

<controls:ToolSessionPanel Title="Gradient" >
        <controls:ToolSessionPanel.Panel>

                    <TextBlock Text="Shape" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding GradientKinds}" SelectedItem="{Binding Kind}" Margin="0,0,0,12" />

                    <controls:ColorPickerField Header="Color A" SelectedColor="{Binding ColorA}" />

                    <controls:ColorPickerField Header="Color B" SelectedColor="{Binding ColorB}" />

                    <StackPanel Visibility="{Binding IsLinear, Converter={StaticResource BoolToVisibilityConverter}}">
                        <controls:SliderField Header="Angle" Value="{Binding Angle}" Minimum="0" Maximum="360" TickFrequency="5" IsSnapToTickEnabled="True" ValueFormat="{}{0:0}°"  />
</StackPanel>

                    <controls:SliderField Header="Opacity" Value="{Binding Opacity}" Minimum="0" Maximum="1" SmallChange="0.05" ValueFormat="{}{0:P0}"  />
</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
