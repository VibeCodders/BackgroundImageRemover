<UserControl x:Class="BackgroundImageRemover.Views.HalftoneToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="◍ HALFTONE" Accent="#FF7C3AED" />

<controls:ToolSessionPanel Title="Halftone" Description="Renders the image as dots on a white background; darker areas get bigger dots, like a printed photograph." >
        <controls:ToolSessionPanel.Panel>

                    <controls:ColorPickerField Header="Dot color" SelectedColor="{Binding DotColor}" />

                    <controls:SliderField Header="Dot size" Value="{Binding CellSize}" Minimum="2" Maximum="16" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
<CheckBox Content="Invert (light = big dots)" IsChecked="{Binding Invert}" Margin="0,2,0,16"
                              Foreground="{DynamicResource Theme.ForegroundBrush}" />

</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
