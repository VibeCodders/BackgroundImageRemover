<UserControl x:Class="BackgroundImageRemover.Views.HealToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             xmlns:models="clr-namespace:BackgroundImageRemover.Models"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="InpaintMethodValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="cv:InpaintMethod" xmlns:cv="clr-namespace:OpenCvSharp;assembly=OpenCvSharp" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="🩹 HEAL &amp; REPAIR" Accent="#FFDC2626" />

        <StatusBar DockPanel.Dock="Bottom" Background="{DynamicResource Theme.BarBackground}" TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
            <StatusBarItem><TextBlock Text="{Binding StatusMessage}" /></StatusBarItem>
        </StatusBar>

        <Border DockPanel.Dock="Right" Width="300" Padding="14" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1,0,0,0" Background="{DynamicResource Theme.PanelBackground}" TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Heal &amp; Repair" FontSize="15" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource Theme.ForegroundBrush}" />

                    <TextBlock Text="Paint blemishes on the image, then heal:" FontWeight="SemiBold" Margin="0,0,0,4" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Brush size" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding BrushRadius, StringFormat='{}{0:0}px'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="2" Maximum="150" Value="{Binding BrushRadius}" Margin="0,2,0,8" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Heal radius" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding HealRadius, StringFormat='{}{0:0}px'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0.1" Maximum="30" Value="{Binding HealRadius}" Margin="0,2,0,8" />

                    <TextBlock Text="Method" FontWeight="SemiBold" Margin="0,4,0,4" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource InpaintMethodValues}}" SelectedItem="{Binding InpaintMethod}" Margin="0,0,0,8" />

                    <Button Content="✕ Clear painted mask" Command="{Binding ClearMaskCommand}" Margin="0,0,0,16" />

                    <Separator Margin="0,0,0,12" />
                    <TextBlock Text="Whole-image repair" FontWeight="SemiBold" Margin="0,0,0,8" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Dust removal" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding RemoveDustKernel}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0" Maximum="15" Value="{Binding RemoveDustKernel}" TickFrequency="2" IsSnapToTickEnabled="True" Margin="0,2,0,8" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Scratch removal" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding RemoveScratchesStrength, StringFormat='{}{0:P0}'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0" Maximum="1" Value="{Binding RemoveScratchesStrength}" SmallChange="0.05" Margin="0,2,0,8" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Surface smooth" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding SurfaceSmoothStrength, StringFormat='{}{0:P0}'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0" Maximum="1" Value="{Binding SurfaceSmoothStrength}" SmallChange="0.05" Margin="0,2,0,8" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Detail enhance" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding DetailEnhanceStrength, StringFormat='{}{0:P0}'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0" Maximum="1" Value="{Binding DetailEnhanceStrength}" SmallChange="0.05" />
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Margin="8">
            <controls:ImagePreviewControl x:Name="HealPreview"
                                           ImageSource="{Binding ResultBitmap}"
                                           Mode="{Binding ResultMode}"
                                           BrushRadius="{Binding BrushRadius}"
                                           StrokeStart="HealPreview_StrokeStart"
                                           StrokeMove="HealPreview_StrokeMove"
                                           StrokeEnd="HealPreview_StrokeEnd" />
        </Grid>
    </DockPanel>
</UserControl>
