<views:BrushStrokeSessionViewBase x:Class="BackgroundImageRemover.Views.LassoSelectToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:BackgroundImageRemover.Views"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             xmlns:models="clr-namespace:BackgroundImageRemover.Models"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader DockPanel.Dock="Top" Badge="◈ LASSO" Accent="#FF0EA5E9" />

<controls:ToolSessionPanel Title="Lasso Select" PanelWidth="280" Description="Draw a freehand outline on the left. The area inside becomes the cutout -- toggle Invert to keep the outside instead."  ShowReset="False">
        <controls:ToolSessionPanel.Panel>
<StackPanel>

                    <CheckBox Content="Invert (keep outside)" IsChecked="{Binding InvertSelection}" Margin="0,0,0,12" />

                    <controls:SliderField Header="Edge feather" Value="{Binding FeatherPixels}" Minimum="0" Maximum="40"  />
<Button Content="Clear selection" Command="{Binding ClearSelectionCommand}" />
                </StackPanel>
</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" /><ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="0" Margin="8">
                <TextBlock DockPanel.Dock="Top" Text="Drag to draw a freehand selection" FontWeight="Bold" Margin="0,0,0,4" />
                <controls:ImagePreviewControl x:Name="LassoPreview"
                                               ImageSource="{Binding SourceBitmap}"
                                               Mode="{x:Static models:InteractionMode.Lasso}"
                                               StrokeStart="OnStrokeStart"
                                               StrokeMove="OnStrokeMove"
                                               StrokeEnd="OnStrokeEnd" />
            </DockPanel>
            <DockPanel Grid.Column="1" Margin="8">
                <TextBlock DockPanel.Dock="Top" Text="Result" FontWeight="Bold" Margin="0,0,0,4" />
                <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                    <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
                </Border>
            </DockPanel>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</views:BrushStrokeSessionViewBase>
