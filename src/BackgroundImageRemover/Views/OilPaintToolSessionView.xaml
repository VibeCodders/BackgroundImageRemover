<UserControl x:Class="BackgroundImageRemover.Views.OilPaintToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="🖌 OIL PAINT" Accent="#FFB45309" />

<controls:ToolSessionPanel Title="Oil Paint" Description="Replaces each area with the dominant colour of its neighbourhood, like thick brush strokes." >
        <controls:ToolSessionPanel.Panel>

                    <controls:SliderField Header="Brush size" Value="{Binding BrushSize}" Minimum="1" Maximum="10" TickFrequency="1" IsSnapToTickEnabled="True"  />
<controls:SliderField Header="Detail" Value="{Binding Detail}" Minimum="2" Maximum="32" TickFrequency="1" IsSnapToTickEnabled="True"  />
</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
