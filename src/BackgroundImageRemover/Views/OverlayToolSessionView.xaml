<UserControl x:Class="BackgroundImageRemover.Views.OverlayToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             xmlns:models="clr-namespace:BackgroundImageRemover.Models"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="TextAnchorValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:TextAnchor" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="OverlayBlendModeValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:OverlayBlendMode" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundBrush}" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundBrush}" />
        </Style>
    </UserControl.Resources>

    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="🔲 OVERLAY" Accent="#FF059669" />

        <StatusBar DockPanel.Dock="Bottom" Background="{DynamicResource Theme.BarBackground}" TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
            <StatusBarItem><TextBlock Text="{Binding StatusMessage}" /></StatusBarItem>
        </StatusBar>

        <Border DockPanel.Dock="Right" Width="300" Padding="14" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1,0,0,0" Background="{DynamicResource Theme.PanelBackground}" TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Overlay (logo / sticker)" FontSize="15" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource Theme.ForegroundBrush}" />

                    <Button Content="Choose overlay image..." Command="{Binding PickOverlayCommand}" Margin="0,0,0,12" />

                    <TextBlock Text="Position" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource TextAnchorValues}}" SelectedItem="{Binding Anchor}" Margin="0,0,0,12" />

                    <controls:SliderField Header="Scale" Value="{Binding Scale}" Minimum="0.01" Maximum="2" ValueFormat="{}{0:P0}"  />
<controls:SliderField Header="Opacity" Value="{Binding Opacity}" Minimum="0" Maximum="1" ValueFormat="{}{0:P0}"  />
<controls:SliderField Header="Margin" Value="{Binding Margin}" Minimum="0" Maximum="200" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
<controls:SliderField Header="Rotation" Value="{Binding Rotation}" Minimum="-180" Maximum="180" ValueFormat="{}{0:0}°"  />
<CheckBox Content="Flip horizontally" IsChecked="{Binding FlipHorizontal}" Margin="0,0,0,4" />
                    <CheckBox Content="Flip vertically" IsChecked="{Binding FlipVertical}" Margin="0,0,0,12" />

                    <TextBlock Text="Blend mode" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource OverlayBlendModeValues}}" SelectedItem="{Binding Blend}" Margin="0,0,0,12" />

                    <CheckBox Content="Color tint" IsChecked="{Binding TintEnabled}" Margin="0,0,0,8" />
                    <StackPanel Visibility="{Binding TintEnabled, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                <TextBlock Text="R" />
                                <Slider Minimum="0" Maximum="255" Value="{Binding TintRed}" TickFrequency="1" IsSnapToTickEnabled="True" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="4,0,4,0">
                                <TextBlock Text="G" />
                                <Slider Minimum="0" Maximum="255" Value="{Binding TintGreen}" TickFrequency="1" IsSnapToTickEnabled="True" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" Margin="4,0,0,0">
                                <TextBlock Text="B" />
                                <Slider Minimum="0" Maximum="255" Value="{Binding TintBlue}" TickFrequency="1" IsSnapToTickEnabled="True" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <CheckBox Content="Drop shadow" IsChecked="{Binding DropShadow}" Margin="0,0,0,8" />

                    <StackPanel Visibility="{Binding DropShadow, Converter={StaticResource BoolToVisibilityConverter}}">
                        <controls:SliderField Header="Shadow offset" Value="{Binding ShadowOffset}" Minimum="0" Maximum="40" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
<controls:SliderField Header="Shadow opacity" Value="{Binding ShadowOpacity}" Minimum="0" Maximum="1" ValueFormat="{}{0:P0}"  />
</StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
