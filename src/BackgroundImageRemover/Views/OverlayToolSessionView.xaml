<UserControl x:Class="BackgroundImageRemover.Views.OverlayToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:BackgroundImageRemover.Models"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="TextAnchorValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:TextAnchor" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="OverlayBlendModeValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:OverlayBlendMode" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#FF1E1E24" Padding="12,8" BorderBrush="#FF33333D" BorderThickness="0,0,0,1">
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" DockPanel.Dock="Left">
                    <Border Background="#FF059669" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                        <TextBlock Text="🔲 OVERLAY" Foreground="White" FontWeight="Bold" FontSize="11" />
                    </Border>
                    <TextBlock Text="{Binding Title}" Foreground="#FFDDDDDD" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <Button Content="✕ Cancel (Esc)" Command="{Binding CancelCommand}" Padding="12,6" Margin="0,0,8,0" Background="#FF2D2D35" Foreground="#FFCCCCCC" BorderBrush="#FF444450" BorderThickness="1" Cursor="Hand" FontWeight="SemiBold" />
                    <Button Content="✓ Apply (Enter)" Command="{Binding ApplyCommand}" Padding="16,6" Background="#FF059669" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="Bold" />
                </StackPanel>
            </DockPanel>
        </Border>

        <StatusBar DockPanel.Dock="Bottom" Background="#FFF5F6F8">
            <StatusBarItem><TextBlock Text="{Binding StatusMessage}" /></StatusBarItem>
        </StatusBar>

        <Border DockPanel.Dock="Right" Width="300" Padding="14" BorderBrush="#FFCCCCCC" BorderThickness="1,0,0,0" Background="#FFFAFAFA">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Overlay (logo / sticker)" FontSize="15" FontWeight="Bold" Margin="0,0,0,8" Foreground="#FF222222" />

                    <Button Content="Choose overlay image..." Command="{Binding PickOverlayCommand}" Margin="0,0,0,12" />

                    <TextBlock Text="Position" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource TextAnchorValues}}" SelectedItem="{Binding Anchor}" Margin="0,0,0,12" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Scale" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding Scale, StringFormat='{}{0:P0}'}" Foreground="#FF666666" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0.01" Maximum="2" Value="{Binding Scale}" Margin="0,2,0,12" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Opacity" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding Opacity, StringFormat='{}{0:P0}'}" Foreground="#FF666666" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0" Maximum="1" Value="{Binding Opacity}" Margin="0,2,0,12" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Margin" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding Margin, StringFormat='{}{0}px'}" Foreground="#FF666666" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0" Maximum="200" Value="{Binding Margin}" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,2,0,12" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Rotation" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding Rotation, StringFormat='{}{0:0}°'}" Foreground="#FF666666" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="-180" Maximum="180" Value="{Binding Rotation}" Margin="0,2,0,12" />

                    <CheckBox Content="Flip horizontally" IsChecked="{Binding FlipHorizontal}" Margin="0,0,0,4" />
                    <CheckBox Content="Flip vertically" IsChecked="{Binding FlipVertical}" Margin="0,0,0,12" />

                    <TextBlock Text="Blend mode" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource OverlayBlendModeValues}}" SelectedItem="{Binding Blend}" Margin="0,0,0,12" />

                    <CheckBox Content="Color tint" IsChecked="{Binding TintEnabled}" Margin="0,0,0,8" />
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,4,0">
                            <TextBlock Text="R" />
                            <Slider Minimum="0" Maximum="255" Value="{Binding TintRed}" TickFrequency="1" IsSnapToTickEnabled="True" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="4,0,4,0">
                            <TextBlock Text="G" />
                            <Slider Minimum="0" Maximum="255" Value="{Binding TintGreen}" TickFrequency="1" IsSnapToTickEnabled="True" />
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="4,0,0,0">
                            <TextBlock Text="B" />
                            <Slider Minimum="0" Maximum="255" Value="{Binding TintBlue}" TickFrequency="1" IsSnapToTickEnabled="True" />
                        </StackPanel>
                    </Grid>

                    <CheckBox Content="Drop shadow" IsChecked="{Binding DropShadow}" Margin="0,0,0,8" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Shadow offset" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding ShadowOffset, StringFormat='{}{0}px'}" Foreground="#FF666666" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0" Maximum="40" Value="{Binding ShadowOffset}" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,2,0,12" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Shadow opacity" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding ShadowOpacity, StringFormat='{}{0:P0}'}" Foreground="#FF666666" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="0" Maximum="1" Value="{Binding ShadowOpacity}" Margin="0,2,0,12" />
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <Image Source="{Binding ResultBitmap}" Stretch="Uniform" />
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
