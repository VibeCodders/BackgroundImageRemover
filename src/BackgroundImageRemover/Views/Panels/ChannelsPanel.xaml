<UserControl x:Class="BackgroundImageRemover.Views.Panels.ChannelsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:BackgroundImageRemover.Models"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="280">
    
    <DockPanel>
        <!-- Header -->
        <Border DockPanel.Dock="Top" Background="{DynamicResource Theme.PanelHeaderBackground}" 
                Padding="10,8" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel>
                <TextBlock Text="Channels" FontWeight="Bold" Foreground="{DynamicResource Theme.ForegroundBrush}" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="AddChannelButton" Content="+" Width="24" Height="24" Margin="2,0" 
                            Click="AddChannelButton_Click" ToolTip="Add new channel" />
                    <Button x:Name="DeleteChannelButton" Content="×" Width="24" Height="24" Margin="2,0" 
                            Click="DeleteChannelButton_Click" ToolTip="Delete channel" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Channels List -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Channels}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:Channel}">
                        <Border Background="{DynamicResource Theme.PanelBackground}" 
                                BorderBrush="{DynamicResource Theme.BorderBrush}" 
                                BorderThickness="0,0,0,1" 
                                Padding="8,6"
                                Margin="0,0,0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Visibility Eye -->
                                <Button Grid.Column="0" Width="20" Height="20" Margin="0,0,4,0"
                                        Background="Transparent" BorderThickness="0"
                                        Content="👁" FontSize="12"
                                        ToolTip="Toggle visibility">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Opacity" Value="0.5" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsVisible}" Value="True">
                                                    <Setter Property="Opacity" Value="1.0" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <!-- Channel Thumbnail -->
                                <Border Grid.Column="1" Width="32" Height="32" Background="Transparent" 
                                        BorderBrush="{DynamicResource Theme.BorderBrush}" 
                                        BorderThickness="1" Margin="0,0,6,0">
                                    <Image Source="{Binding Thumbnail}" Stretch="Uniform" 
                                           Width="30" Height="30" />
                                </Border>

                                <!-- Channel Name -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" 
                                               Foreground="{DynamicResource Theme.ForegroundBrush}" />
                                    <TextBlock Text="{Binding ChannelType}" FontSize="10" 
                                               Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>