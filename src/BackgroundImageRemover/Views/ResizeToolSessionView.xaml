<UserControl x:Class="BackgroundImageRemover.Views.ResizeToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:BackgroundImageRemover.Models"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="ResampleMethodValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:ResampleMethod" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#FF1E1E24" Padding="12,8" BorderBrush="#FF33333D" BorderThickness="0,0,0,1">
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" DockPanel.Dock="Left">
                    <Border Background="#FF0891B2" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                        <TextBlock Text="⤡ RESIZE" Foreground="White" FontWeight="Bold" FontSize="11" />
                    </Border>
                    <TextBlock Text="{Binding Title}" Foreground="#FFDDDDDD" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <Button Content="✕ Cancel (Esc)" Command="{Binding CancelCommand}" Padding="12,6" Margin="0,0,8,0" Background="#FF2D2D35" Foreground="#FFCCCCCC" BorderBrush="#FF444450" BorderThickness="1" Cursor="Hand" FontWeight="SemiBold" />
                    <Button Content="✓ Apply (Enter)" Command="{Binding ApplyCommand}" Padding="16,6" Background="#FF0891B2" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="Bold" />
                </StackPanel>
            </DockPanel>
        </Border>

        <StatusBar DockPanel.Dock="Bottom" Background="#FFF5F6F8">
            <StatusBarItem><TextBlock Text="{Binding StatusMessage}" /></StatusBarItem>
        </StatusBar>

        <Border DockPanel.Dock="Right" Width="300" Padding="14" BorderBrush="#FFCCCCCC" BorderThickness="1,0,0,0" Background="#FFFAFAFA">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Resize" FontSize="15" FontWeight="Bold" Margin="0,0,0,8" Foreground="#FF222222" />

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="Width (px)" FontWeight="SemiBold" />
                            <TextBox Text="{Binding Width, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Height (px)" FontWeight="SemiBold" />
                            <TextBox Text="{Binding Height, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>
                    <CheckBox Content="Lock aspect ratio" IsChecked="{Binding KeepAspect}" Margin="0,0,0,12" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Percent" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding Percent, StringFormat='{}{0:0}%'}" Foreground="#FF666666" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="1" Maximum="400" Value="{Binding Percent}" Margin="0,2,0,12" />

                    <TextBlock Text="Interpolation" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource ResampleMethodValues}}" SelectedItem="{Binding Method}" Margin="0,0,0,12" />

                    <UniformGrid Columns="2" Margin="0,0,0,12">
                        <Button Content="50%" Command="{Binding PresetHalfCommand}" Margin="0,0,4,4" />
                        <Button Content="200%" Command="{Binding PresetDoubleCommand}" Margin="4,0,0,4" />
                        <Button Content="1024 wide" Command="{Binding PresetWidth1024Command}" Margin="0,0,4,0" />
                        <Button Content="1920 wide" Command="{Binding PresetWidth1920Command}" Margin="4,0,0,0" />
                    </UniformGrid>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <Image Source="{Binding ResultBitmap}" Stretch="Uniform" />
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
