<UserControl x:Class="BackgroundImageRemover.Views.ResizeToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             xmlns:models="clr-namespace:BackgroundImageRemover.Models"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="ResampleMethodValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:ResampleMethod" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="ResizeModeValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:ResizeMode" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundBrush}" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundBrush}" />
        </Style>
    </UserControl.Resources>

    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader DockPanel.Dock="Top" Badge="⤡ RESIZE" Accent="#FF0891B2" />

<controls:ToolSessionPanel Title="Resize"  ShowReset="False">
        <controls:ToolSessionPanel.Panel>
<StackPanel>

                    <TextBlock Text="Mode" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource ResizeModeValues}}" SelectedItem="{Binding Mode}" Margin="0,0,0,12" />

                    <StackPanel Visibility="{Binding Mode, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=ExactSize}">
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="Width (px)" FontWeight="SemiBold" />
                                <TextBox Text="{Binding Width, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Height (px)" FontWeight="SemiBold" />
                                <TextBox Text="{Binding Height, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Grid>
                        <CheckBox Content="Lock aspect ratio" IsChecked="{Binding KeepAspect}" Margin="0,0,0,12" />
                    </StackPanel>

                    <StackPanel Visibility="{Binding Mode, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Percent}">
                        <controls:SliderField Header="Percent" Value="{Binding Percent}" Minimum="1" Maximum="400" ValueFormat="{}{0:0}%"  />
</StackPanel>

                    <StackPanel Visibility="{Binding Mode, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=FitWithin|FillTo}">
                        <TextBlock Text="Fit / fill box (px)" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                <TextBlock Text="W" />
                                <TextBox Text="{Binding FitWidth, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="H" />
                                <TextBox Text="{Binding FitHeight, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <StackPanel Visibility="{Binding Mode, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=LongestSide}">
                        <TextBlock Text="Longest side (px)" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox Text="{Binding LongestSide, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
                    </StackPanel>

                    <StackPanel Visibility="{Binding Mode, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Megapixels}">
                        <TextBlock Text="Megapixels" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Megapixels, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
                    </StackPanel>

                    <TextBlock Text="Interpolation" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource ResampleMethodValues}}" SelectedItem="{Binding Method}" Margin="0,0,0,12" />

                    <UniformGrid Columns="2" Margin="0,0,0,12">
                        <Button Content="50%" Command="{Binding PresetHalfCommand}" Margin="0,0,4,4" />
                        <Button Content="200%" Command="{Binding PresetDoubleCommand}" Margin="4,0,0,4" />
                        <Button Content="1024 wide" Command="{Binding PresetWidth1024Command}" Margin="0,0,4,0" />
                        <Button Content="1920 wide" Command="{Binding PresetWidth1920Command}" Margin="4,0,0,0" />
                    </UniformGrid>
                </StackPanel>
</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
