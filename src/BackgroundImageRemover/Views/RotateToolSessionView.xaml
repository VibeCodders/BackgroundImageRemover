<UserControl x:Class="BackgroundImageRemover.Views.RotateToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader DockPanel.Dock="Top" Badge="↺ ROTATE" Accent="#FF7C3AED" />

<controls:ToolSessionPanel Title="Rotate" Description="Rotate by an arbitrary angle around the image center." >
        <controls:ToolSessionPanel.Panel>
<StackPanel>

                    <controls:SliderField Header="Angle" Value="{Binding Angle}" Minimum="-360" Maximum="360" SmallChange="1" ValueFormat="{}{0:0}°"  />
<CheckBox Content="Expand canvas to fit the rotated image" IsChecked="{Binding Expand}" Margin="0,0,0,16" />

</StackPanel>
</controls:ToolSessionPanel.Panel>

    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
