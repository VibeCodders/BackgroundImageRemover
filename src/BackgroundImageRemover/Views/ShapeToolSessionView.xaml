<UserControl x:Class="BackgroundImageRemover.Views.ShapeToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             xmlns:models="clr-namespace:BackgroundImageRemover.Models"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader DockPanel.Dock="Top" Badge="⬜ SHAPE" Accent="#FF10B981" />

<controls:ToolSessionPanel Title="Shape" Description="Drag on the left image to place the shape; drag inside it to move, drag its corners to resize, drag the handle above it to rotate (hold Shift to snap to 5° steps). The controls below let you fine-tune." >
        <controls:ToolSessionPanel.Panel>
<StackPanel>

                    <TextBlock Text="Shape type" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding ShapeKinds}" SelectedItem="{Binding ShapeKind}" Margin="0,0,0,8" />

                    <controls:SliderField Header="Sides / points" Value="{Binding Segments}" Minimum="3" Maximum="20" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}" Visibility="{Binding IsPointShapes, Converter={StaticResource BoolToVisibilityConverter}}"  />
<controls:SliderField Header="Inner radius" Value="{Binding StarRatio}" Minimum="0.05" Maximum="0.95" SmallChange="0.05" ValueFormat="{}{0:P0}" Visibility="{Binding IsStar, Converter={StaticResource BoolToVisibilityConverter}}"  />
<controls:SliderField Header="Position X" Value="{Binding PositionX}" Minimum="0" Maximum="100" SmallChange="1" ValueFormat="{}{0:0}%"  />
<controls:SliderField Header="Position Y" Value="{Binding PositionY}" Minimum="0" Maximum="100" SmallChange="1" ValueFormat="{}{0:0}%"  />
<controls:SliderField Header="Width" Value="{Binding SizeWidth}" Minimum="1" Maximum="100" SmallChange="1" ValueFormat="{}{0:0}%"  />
<controls:SliderField Header="Height" Value="{Binding SizeHeight}" Minimum="1" Maximum="100" SmallChange="1" ValueFormat="{}{0:0}%"  />
<controls:SliderField Header="Rotation" Value="{Binding Rotation}" Minimum="0" Maximum="360" SmallChange="1" ValueFormat="{}{0:0}°"  />
<controls:SliderField Header="Stroke width" Value="{Binding StrokeWidth}" Minimum="0" Maximum="40" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
<controls:ColorPickerField Header="Stroke color" SelectedColor="{Binding StrokeColor}" />

                    <CheckBox Content="Fill shape" IsChecked="{Binding FillEnabled}" Margin="0,0,0,8" FontWeight="SemiBold"
                              Visibility="{Binding SupportsFill, Converter={StaticResource BoolToVisibilityConverter}}" />

                    <controls:ColorPickerField Header="Fill color" SelectedColor="{Binding FillColor}"
                                               Visibility="{Binding FillEnabled, Converter={StaticResource BoolToVisibilityConverter}}" />

                    <controls:SliderField Header="Fill opacity" Value="{Binding FillOpacity}" Minimum="0" Maximum="1" SmallChange="0.05" ValueFormat="{}{0:P0}"  />
</StackPanel>
</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.StatusBarContent>
            <TextBlock x:Name="CursorPositionLabel" Margin="12,0,0,0" />
        </controls:ToolSessionPanel.StatusBarContent>
        <controls:ToolSessionPanel.Preview>


        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" /><ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="0" Margin="8">
                <TextBlock DockPanel.Dock="Top" Text="Drag to place or adjust the shape" FontWeight="Bold" Margin="0,0,0,4" />
                <controls:ImagePreviewControl x:Name="ShapePreview"
                                              ImageSource="{Binding SourceBitmap}"
                                              Mode="{x:Static models:InteractionMode.EditRect}"
                                              RectSelected="ShapePreview_RectSelected" />
            </DockPanel>
            <DockPanel Grid.Column="1" Margin="8">
                <TextBlock DockPanel.Dock="Top" Text="Result" FontWeight="Bold" Margin="0,0,0,4" />
                <Border Style="{StaticResource PreviewBorder}">
                    <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
                </Border>
            </DockPanel>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
