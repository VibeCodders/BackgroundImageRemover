<UserControl x:Class="BackgroundImageRemover.Views.SketchToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="✏️ SKETCH" Accent="#FF6B7280" />

<controls:ToolSessionPanel Title="Sketch" Description="Converts the image into a pencil sketch (luminance divided by its own blur)." >
        <controls:ToolSessionPanel.Panel>

                    <controls:SliderField Header="Softness" Value="{Binding BlurRadius}" Minimum="1" Maximum="50" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
<CheckBox Content="Invert (dark lines on light paper)" IsChecked="{Binding Invert}" Margin="0,0,0,16" />

</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
