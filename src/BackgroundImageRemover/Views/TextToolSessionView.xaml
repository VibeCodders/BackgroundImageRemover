<UserControl x:Class="BackgroundImageRemover.Views.TextToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             xmlns:models="clr-namespace:BackgroundImageRemover.Models"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="TextAnchorValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:TextAnchor" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundBrush}" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundBrush}" />
        </Style>
    </UserControl.Resources>

    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="✎ TEXT" Accent="#FFDB2777" />

        <StatusBar DockPanel.Dock="Bottom" Background="{DynamicResource Theme.BarBackground}" TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
            <StatusBarItem><TextBlock Text="{Binding StatusMessage}" /></StatusBarItem>
        </StatusBar>

        <Border DockPanel.Dock="Right" Width="300" Padding="14" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1,0,0,0" Background="{DynamicResource Theme.PanelBackground}" TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Text Watermark" FontSize="15" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource Theme.ForegroundBrush}" />

                    <TextBlock Text="Text" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                    <controls:SliderField Header="Size" Value="{Binding FontSize}" Minimum="8" Maximum="200" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
<TextBlock Text="Position" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource TextAnchorValues}}" SelectedItem="{Binding Anchor}" Margin="0,0,0,12" />

                    <controls:ColorPickerField Header="Color" SelectedColor="{Binding Color}" />

                    <controls:SliderField Header="Opacity" Value="{Binding Opacity}" Minimum="0" Maximum="1" SmallChange="0.05" LargeChange="0.1" ValueFormat="{}{0:P0}"  />
<controls:SliderField Header="Margin" Value="{Binding Margin}" Minimum="0" Maximum="200" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
<controls:SliderField Header="Letter spacing" Value="{Binding LetterSpacing}" Minimum="0" Maximum="30" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0:0}px"  />
<controls:SliderField Header="Line spacing" Value="{Binding LineSpacing}" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
<CheckBox Content="Auto-fit to image width" IsChecked="{Binding AutoFitWidth}" FontWeight="SemiBold" Margin="0,0,0,8" />

                    <controls:SliderField Header="Rotation" Value="{Binding Rotation}" Minimum="-180" Maximum="180" ValueFormat="{}{0:0}°"  />
<CheckBox Content="Bold" IsChecked="{Binding Bold}" FontWeight="SemiBold" Margin="0,0,0,8" />

                    <TextBlock Text="Outline" FontWeight="SemiBold" Margin="0,4,0,4" />
                    <controls:SliderField Header="Thickness" Value="{Binding OutlineThickness}" Minimum="0" Maximum="10" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
<controls:ColorPickerField Header="Outline color" SelectedColor="{Binding OutlineColor}" />

                    <TextBlock Text="Shadow" FontWeight="SemiBold" Margin="0,4,0,4" />
                    <controls:SliderField Header="Offset" Value="{Binding ShadowOffset}" Minimum="0" Maximum="20" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
<controls:SliderField Header="Blur" Value="{Binding ShadowBlur}" Minimum="0" Maximum="20" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0:0}px"  />
<controls:SliderField Header="Opacity" Value="{Binding ShadowOpacity}" Minimum="0" Maximum="1" TickFrequency="0.05" ValueFormat="{}{0:P0}"  />
<controls:ColorPickerField Header="Shadow color" SelectedColor="{Binding ShadowColor}" />

                    <CheckBox Content="Background plate" IsChecked="{Binding BackgroundPlate}" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <StackPanel Visibility="{Binding BackgroundPlate, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,0,0,12">
                        <controls:ColorPickerField Header="Plate color" SelectedColor="{Binding PlateColor}" />
                        <controls:SliderField Header="Opacity" Value="{Binding PlateOpacity}" Minimum="0" Maximum="1" TickFrequency="0.05" ValueFormat="{}{0:P0}"  />
<controls:SliderField Header="Padding" Value="{Binding PlatePadding}" Minimum="0" Maximum="60" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0}px"  />
</StackPanel>

                    <Button Content="✕ Clear text" Command="{Binding ClearTextCommand}" Height="30" Background="{DynamicResource Theme.PanelHeaderBackground}" Foreground="{DynamicResource Theme.ForegroundBrush}" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1" />
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
