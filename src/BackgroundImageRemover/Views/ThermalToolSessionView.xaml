<UserControl x:Class="BackgroundImageRemover.Views.ThermalToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader DockPanel.Dock="Top" Badge="🌡 THERMAL" Accent="#FFDC2626" />

<controls:ToolSessionPanel Title="Thermal" Description="Maps image brightness onto a blue-to-red heat palette, like an infrared camera view." >
        <controls:ToolSessionPanel.Panel>
<StackPanel>

                    <controls:SliderField Header="Intensity" Value="{Binding Intensity}" Minimum="0" Maximum="2" SmallChange="0.05" ValueFormat="{}{0:0.0}"  />
<CheckBox Content="Invert (cold = bright)" IsChecked="{Binding Invert}" Margin="0,2,0,16"
                              Foreground="{DynamicResource Theme.ForegroundBrush}" />

</StackPanel>
</controls:ToolSessionPanel.Panel>

    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
