<UserControl x:Class="BackgroundImageRemover.Views.TiltShiftToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundBrush}" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundBrush}" />
        </Style>
    </UserControl.Resources>
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader Badge="📐 TILT-SHIFT" Accent="#FF4F46E5" />

<controls:ToolSessionPanel Title="Miniature Effect" >
        <controls:ToolSessionPanel.Panel>

                    <controls:SliderField Header="Focus center" Value="{Binding FocusCenter}" Minimum="0" Maximum="1" SmallChange="0.01" ValueFormat="{}{0:P0}"  />
<controls:SliderField Header="Focus width" Value="{Binding FocusWidth}" Minimum="0" Maximum="1" SmallChange="0.01" ValueFormat="{}{0:P0}"  />
<controls:SliderField Header="Blur radius" Value="{Binding BlurRadius}" Minimum="0" Maximum="60" TickFrequency="1" IsSnapToTickEnabled="True" ValueFormat="{}{0:0}px"  />
<controls:SliderField Header="Saturation boost" Value="{Binding SaturationBoost}" Minimum="-1" Maximum="2" SmallChange="0.05" ValueFormat="{}{0:0.00}"  />
<CheckBox Content="Vertical band" IsChecked="{Binding Vertical}" FontWeight="SemiBold" Margin="0,0,0,16" />

</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
