<UserControl x:Class="BackgroundImageRemover.Views.TransformToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <Border DockPanel.Dock="Top" Background="{DynamicResource Theme.BarBackground}" Padding="12,8" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" DockPanel.Dock="Left">
                    <Border Background="#FF2563EB" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                        <TextBlock Text="↻ TRANSFORM" Foreground="White" FontWeight="Bold" FontSize="11" />
                    </Border>
                    <TextBlock Text="{Binding Title}" Foreground="{DynamicResource Theme.ForegroundBrush}" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <Button Content="✕ Cancel (Esc)" Command="{Binding CancelCommand}" Padding="12,6" Margin="0,0,8,0" Background="{DynamicResource Theme.PanelBackground}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1" Cursor="Hand" FontWeight="SemiBold" />
                    <Button Content="✓ Apply (Enter)" Command="{Binding ApplyCommand}" Padding="16,6" Background="#FF2563EB" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="Bold" />
                </StackPanel>
            </DockPanel>
        </Border>

        <StatusBar DockPanel.Dock="Bottom" Background="{DynamicResource Theme.BarBackground}" TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
            <StatusBarItem><TextBlock Text="{Binding StatusMessage}" /></StatusBarItem>
        </StatusBar>

        <Border DockPanel.Dock="Right" Width="300" Padding="14" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1,0,0,0" Background="{DynamicResource Theme.PanelBackground}" TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Transform" FontSize="15" FontWeight="Bold" Margin="0,0,0,8" Foreground="{DynamicResource Theme.ForegroundBrush}" />

                    <TextBlock Text="Quick flips &amp; rotations" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <UniformGrid Columns="2">
                        <Button Content="↔ Flip H" Command="{Binding FlipHorizontalCommand}" Margin="0,0,4,4" />
                        <Button Content="↕ Flip V" Command="{Binding FlipVerticalCommand}" Margin="4,0,0,4" />
                        <Button Content="⟳ Rotate 90° CW" Command="{Binding Rotate90ClockwiseCommand}" Margin="0,0,4,4" />
                        <Button Content="⟲ Rotate 90° CCW" Command="{Binding Rotate90CounterClockwiseCommand}" Margin="4,0,0,4" />
                        <Button Content="↻ Rotate 180°" Command="{Binding Rotate180Command}" Grid.ColumnSpan="2" Margin="0,0,0,12" />
                    </UniformGrid>

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Rotate angle" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding Angle, StringFormat='{}{0:0}°'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="-180" Maximum="180" Value="{Binding Angle}" Margin="0,2,0,4" />
                    <Button Content="Apply rotation" Command="{Binding RotateByAngleCommand}" Margin="0,0,0,12" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Scale" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding ScalePercent, StringFormat='{}{0:0}%'}" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="10" Maximum="400" Value="{Binding ScalePercent}" Margin="0,2,0,4" />
                    <Button Content="Apply scale" Command="{Binding ResizeCommand}" Margin="0,0,0,16" />

                    <TextBlock Text="Skew" FontWeight="SemiBold" Margin="0,4,0,4" />
                    <DockPanel Margin="0,0,0,4">
                        <TextBlock Text="Horizontal" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding SkewX, StringFormat='{}{0:0}°'}" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="-45" Maximum="45" Value="{Binding SkewX}" Margin="0,0,0,4" />
                    <DockPanel Margin="0,0,0,4">
                        <TextBlock Text="Vertical" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding SkewY, StringFormat='{}{0:0}°'}" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="-45" Maximum="45" Value="{Binding SkewY}" Margin="0,0,0,4" />
                    <Button Content="Apply skew" Command="{Binding ApplySkewCommand}" Margin="0,0,0,16" />

                    <TextBlock Text="Exact size (px)" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,4,0">
                            <TextBlock Text="Width" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                            <TextBox Text="{Binding ExactWidth, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Height" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                            <TextBox Text="{Binding ExactHeight, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>
                    <Button Content="Apply exact size" Command="{Binding ApplyExactResizeCommand}" Margin="0,0,0,16" />

                    <TextBlock Text="Crop to aspect ratio (W:H)" FontWeight="SemiBold" Margin="0,4,0,4" />
                    <Slider Minimum="0.25" Maximum="3" Value="{Binding CropAspectRatio}" SmallChange="0.05" LargeChange="0.25" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding CropAspectRatio, StringFormat='{}{0:0.00}'}" FontSize="11" Foreground="{DynamicResource Theme.MutedForegroundBrush}" HorizontalAlignment="Center" Margin="0,0,0,4" />
                    <Button Content="Apply crop" Command="{Binding ApplyCropAspectCommand}" Margin="0,0,0,8" />

                    <Button Content="✂ Trim flat border" Command="{Binding TrimBorderCommand}" Margin="0,0,0,12" />

                    <Button Content="📐 Auto-straighten" Command="{Binding AutoStraightenCommand}" Margin="0,0,0,16" />

                    <TextBlock Text="Expand canvas (padding px)" FontWeight="SemiBold" Margin="0,4,0,4" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,2,0">
                            <TextBlock Text="Left" FontSize="10" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                            <TextBox Text="{Binding PadLeft, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,2,0">
                            <TextBlock Text="Top" FontSize="10" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                            <TextBox Text="{Binding PadTop, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="0,0,2,0">
                            <TextBlock Text="Right" FontSize="10" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                            <TextBox Text="{Binding PadRight, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="3">
                            <TextBlock Text="Bottom" FontSize="10" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                            <TextBox Text="{Binding PadBottom, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>
                    <Button Content="Apply padding" Command="{Binding ApplyPaddingCommand}" Margin="0,0,0,16" />

                    <TextBlock Text="Fit within box (px, keeps aspect)" FontWeight="SemiBold" Margin="0,4,0,4" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,4,0">
                            <TextBlock Text="Max width" FontSize="10" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                            <TextBox Text="{Binding FitWidth, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Max height" FontSize="10" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                            <TextBox Text="{Binding FitHeight, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>
                    <Button Content="Apply fit" Command="{Binding ApplyFitCommand}" Margin="0,0,0,16" />

                    <TextBlock Text="Center crop to size (px)" FontWeight="SemiBold" Margin="0,4,0,4" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,4,0">
                            <TextBlock Text="Width" FontSize="10" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                            <TextBox Text="{Binding CenterCropWidth, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Height" FontSize="10" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                            <TextBox Text="{Binding CenterCropHeight, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>
                    <Button Content="Apply center crop" Command="{Binding ApplyCenterCropCommand}" Margin="0,0,0,16" />

                    <TextBlock Text="Tile to size (px)" FontWeight="SemiBold" Margin="0,4,0,4" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,4,0">
                            <TextBlock Text="Width" FontSize="10" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                            <TextBox Text="{Binding TileWidth, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Height" FontSize="10" Foreground="{DynamicResource Theme.MutedForegroundBrush}" />
                            <TextBox Text="{Binding TileHeight, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>
                    <Button Content="Apply tile" Command="{Binding ApplyTileCommand}" Margin="0,0,0,16" />

                    <Button Content="↺ Reset" Command="{Binding ResetCommand}" Height="30" Background="{DynamicResource Theme.PanelHeaderBackground}" Foreground="{DynamicResource Theme.ForegroundBrush}" BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1" />
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <Image Source="{Binding ResultBitmap}" Stretch="Uniform" />
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
