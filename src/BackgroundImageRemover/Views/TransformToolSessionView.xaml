<UserControl x:Class="BackgroundImageRemover.Views.TransformToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#FF1E1E24" Padding="12,8" BorderBrush="#FF33333D" BorderThickness="0,0,0,1">
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" DockPanel.Dock="Left">
                    <Border Background="#FF2563EB" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                        <TextBlock Text="↻ TRANSFORM" Foreground="White" FontWeight="Bold" FontSize="11" />
                    </Border>
                    <TextBlock Text="{Binding Title}" Foreground="#FFDDDDDD" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <Button Content="✕ Cancel (Esc)" Command="{Binding CancelCommand}" Padding="12,6" Margin="0,0,8,0" Background="#FF2D2D35" Foreground="#FFCCCCCC" BorderBrush="#FF444450" BorderThickness="1" Cursor="Hand" FontWeight="SemiBold" />
                    <Button Content="✓ Apply (Enter)" Command="{Binding ApplyCommand}" Padding="16,6" Background="#FF2563EB" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="Bold" />
                </StackPanel>
            </DockPanel>
        </Border>

        <StatusBar DockPanel.Dock="Bottom" Background="#FFF5F6F8">
            <StatusBarItem><TextBlock Text="{Binding StatusMessage}" /></StatusBarItem>
        </StatusBar>

        <Border DockPanel.Dock="Right" Width="300" Padding="14" BorderBrush="#FFCCCCCC" BorderThickness="1,0,0,0" Background="#FFFAFAFA">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Transform" FontSize="15" FontWeight="Bold" Margin="0,0,0,8" Foreground="#FF222222" />

                    <TextBlock Text="Quick flips &amp; rotations" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <UniformGrid Columns="2">
                        <Button Content="↔ Flip H" Command="{Binding FlipHorizontalCommand}" Margin="0,0,4,4" />
                        <Button Content="↕ Flip V" Command="{Binding FlipVerticalCommand}" Margin="4,0,0,4" />
                        <Button Content="⟳ Rotate 90° CW" Command="{Binding Rotate90ClockwiseCommand}" Margin="0,0,4,4" />
                        <Button Content="⟲ Rotate 90° CCW" Command="{Binding Rotate90CounterClockwiseCommand}" Margin="4,0,0,4" />
                        <Button Content="↻ Rotate 180°" Command="{Binding Rotate180Command}" Grid.ColumnSpan="2" Margin="0,0,0,12" />
                    </UniformGrid>

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Rotate angle" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding Angle, StringFormat='{}{0:0}°'}" Foreground="#FF666666" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="-180" Maximum="180" Value="{Binding Angle}" Margin="0,2,0,4" />
                    <Button Content="Apply rotation" Command="{Binding RotateByAngleCommand}" Margin="0,0,0,12" />

                    <DockPanel Margin="0,4,0,0">
                        <TextBlock Text="Scale" FontWeight="SemiBold" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding ScalePercent, StringFormat='{}{0:0}%'}" Foreground="#FF666666" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="10" Maximum="400" Value="{Binding ScalePercent}" Margin="0,2,0,4" />
                    <Button Content="Apply scale" Command="{Binding ResizeCommand}" Margin="0,0,0,16" />

                    <TextBlock Text="Skew" FontWeight="SemiBold" Margin="0,4,0,4" />
                    <DockPanel Margin="0,0,0,4">
                        <TextBlock Text="Horizontal" FontSize="11" Foreground="#FF666666" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding SkewX, StringFormat='{}{0:0}°'}" FontSize="11" Foreground="#FF666666" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="-45" Maximum="45" Value="{Binding SkewX}" Margin="0,0,0,4" />
                    <DockPanel Margin="0,0,0,4">
                        <TextBlock Text="Vertical" FontSize="11" Foreground="#FF666666" DockPanel.Dock="Left" />
                        <TextBlock Text="{Binding SkewY, StringFormat='{}{0:0}°'}" FontSize="11" Foreground="#FF666666" DockPanel.Dock="Right" />
                    </DockPanel>
                    <Slider Minimum="-45" Maximum="45" Value="{Binding SkewY}" Margin="0,0,0,4" />
                    <Button Content="Apply skew" Command="{Binding ApplySkewCommand}" Margin="0,0,0,16" />

                    <TextBlock Text="Exact size (px)" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,4,0">
                            <TextBlock Text="Width" FontSize="11" Foreground="#FF666666" />
                            <TextBox Text="{Binding ExactWidth, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Height" FontSize="11" Foreground="#FF666666" />
                            <TextBox Text="{Binding ExactHeight, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>
                    <Button Content="Apply exact size" Command="{Binding ApplyExactResizeCommand}" Margin="0,0,0,16" />

                    <TextBlock Text="Crop to aspect ratio (W:H)" FontWeight="SemiBold" Margin="0,4,0,4" />
                    <Slider Minimum="0.25" Maximum="3" Value="{Binding CropAspectRatio}" SmallChange="0.05" LargeChange="0.25" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding CropAspectRatio, StringFormat='{}{0:0.00}'}" FontSize="11" Foreground="#FF666666" HorizontalAlignment="Center" Margin="0,0,0,4" />
                    <Button Content="Apply crop" Command="{Binding ApplyCropAspectCommand}" Margin="0,0,0,8" />

                    <Button Content="✂ Trim flat border" Command="{Binding TrimBorderCommand}" Margin="0,0,0,16" />

                    <Button Content="↺ Reset" Command="{Binding ResetCommand}" Height="30" Background="#FFF0F0F0" Foreground="#FF333333" BorderBrush="#FFCCCCCC" BorderThickness="1" />
                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <Image Source="{Binding ResultBitmap}" Stretch="Uniform" />
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
