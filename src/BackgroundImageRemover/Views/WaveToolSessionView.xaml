<UserControl x:Class="BackgroundImageRemover.Views.WaveToolSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:BackgroundImageRemover.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <DockPanel TextElement.Foreground="{DynamicResource Theme.ForegroundBrush}">
        <controls:ToolSessionHeader DockPanel.Dock="Top" Badge="〰 WAVE" Accent="#FF0284C7" />

<controls:ToolSessionPanel Title="Wave" Description="Displaces the pixels with a sine wave; the angle rotates the direction of the ridges." >
        <controls:ToolSessionPanel.Panel>
<StackPanel>

                    <controls:SliderField Header="Amplitude" Value="{Binding Amplitude}" Minimum="0" Maximum="60" SmallChange="1" ValueFormat="{}{0:0}px"  />
<controls:SliderField Header="Wavelength" Value="{Binding Wavelength}" Minimum="10" Maximum="400" SmallChange="5" ValueFormat="{}{0:0}px"  />
<controls:SliderField Header="Angle" Value="{Binding Angle}" Minimum="0" Maximum="360" TickFrequency="5" IsSnapToTickEnabled="True" ValueFormat="{}{0:0}°"  />
</StackPanel>
</controls:ToolSessionPanel.Panel>

        <controls:ToolSessionPanel.Preview>


        <Grid Margin="8">
            <Border BorderBrush="#FF3A3A3A" BorderThickness="1">
                <controls:ZoomableImageControl ImageSource="{Binding ResultBitmap}" />
            </Border>
        </Grid>
    
        </controls:ToolSessionPanel.Preview>
    </controls:ToolSessionPanel>
</DockPanel>
</UserControl>
